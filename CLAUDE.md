# CLAUDE.md

`AGENTS.md` is the canonical agent guidance file for this repository.

Claude Code and other agents should read `AGENTS.md` and follow it as the source
of truth. Keep this file as a compatibility pointer so guidance does not drift
between agent-specific files.
