# agent.md

`AGENTS.md` is the canonical agent guidance file for this repository.

Agents that look for lowercase `agent.md` should read `AGENTS.md` and follow it
as the source of truth. Keep this file as a compatibility pointer so guidance
does not drift between agent-specific files.
